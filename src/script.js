let number = 7; //assigning a value to a variable
if(number % 2 == 0){ //using the if keyword,adding a condition to the if keyword to check if the condition is met
    // alert(`${number} is even `) 
    console.log(`${number} is even`) //logging the output to the console if the condition is met
};

if(number%2==0){
    // alert(number + "is even")
    console.log(number + "is even")
} else {
    console.log(`${number} is odd`)
}

